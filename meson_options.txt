option(
  'build_tests',
  type: 'boolean',
  value: false,
  description: 'Build the unit test suite',
)

option(
  'lookup_strategy',
  type: 'combo',
  choices: ['linear', 'hash'],
  value: 'linear',
  description: 'Slot lookup strategy: linear scan or hash-based round-robin',
)

option(
  'pool_max_slots',
  type: 'integer',
  value: 16,
  min: 1,
  description: 'Maximum number of pool slots (POOL_MAX_SLOTS)',
)

option(
  'pool_item_size',
  type: 'integer',
  value: 64,
  min: 1,
  description: 'Size in bytes of each pool slot (POOL_ITEM_SIZE)',
)

option(
  'atomicity_mode',
  type: 'combo',
  choices: ['auto', 'c11', 'volatile'],
  value: 'auto',
  description: 'Concurrency model for slot_status flags: auto (detect from toolchain), c11 (_Atomic), or volatile (single-core MCU, e.g. TI C2000)',
)

option(
  'tsan',
  type: 'boolean',
  value: false,
  description: 'Build the concurrency stress test under ThreadSanitizer (requires libtsan, c11 atomicity mode)',
)
